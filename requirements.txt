# Scraper engines — versions intentionally loose so crawl4ai's transitive
# deps (playwright==1.47.0, lxml==5.3.0, requests<2.32.3) can resolve
playwright>=1.44.0
playwright-stealth>=1.0.6
scrapy>=2.11.0
beautifulsoup4>=4.12.0
requests>=2.28.0,<2.32.3
crawl4ai==0.3.7

# HTTP client — curl_cffi gives browser-grade TLS fingerprints (JA3/JA4)
httpx>=0.27.0
curl_cffi>=0.7.0

# Scheduler
apscheduler==3.10.4

# API / web server
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Database
sqlalchemy==2.0.30
aiosqlite==0.20.0
asyncpg==0.29.0

# Utilities
python-dotenv==1.0.1
lxml>=5.2.2
fake-useragent>=1.5.1

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
